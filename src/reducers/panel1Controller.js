// here we will controller all the activities which will be related to the find opportunities panel1 part ;

const initialState = {
    
}

const panel1Controller = (state = initialState , action ) =>{
    
}

export default panel1Controller ;